import React, { PropTypes } from 'react'
import Todo from './Todo'

const TodoList = ({ todos, onTodoClick }) => (
  <ul>
    {todos.map(todo => (
      <Todo
      // YOU ARE HERE
      // YOU ARE HERE
      // YOU ARE HERE
      // YOU ARE HERE
    ))}
  </ul>
)
